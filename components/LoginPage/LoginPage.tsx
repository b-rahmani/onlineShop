import classes from "./login.module.scss";
const LoginPage = () => {
  return (
    <div className={classes.loginPage}>
      {/* avatar side */}
      this is image side
      {/* forms side */}
      this is form side
    </div>
  );
};
export default LoginPage;
